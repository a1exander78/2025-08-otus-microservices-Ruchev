apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name:  hw06-monitor
  labels:
    app: hw06-app
    release: prometheus
    
spec:
  endpoints:
    - port: http
      scrapeTimeout: 30s
      path: /actuator/prometheus     
      honorLabels: true
      scheme: http
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: hw06-app
  
